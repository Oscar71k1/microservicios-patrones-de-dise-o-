{
  "name": "sistema-inscripciones-frontend",
  "version": "1.0.0",
  "description": "Frontend minimalista para sistema de gestión de inscripciones y pagos",
  "main": "index.html",
  "scripts": {
    "start": "node server.js",
    "dev": "npx live-server --port=3003 --no-browser",
    "serve": "npx http-server . -p 3003 -c-1",
    "test": "echo 'No tests configured yet'",
    "test:coverage": "echo 'No tests configured yet'",
    "lint": "eslint . || echo 'ESLint not configured'",
    "lint:fix": "eslint . --fix || echo 'ESLint not configured'",
    "build": "echo 'Build completed - no build process needed'",
    "docs": "echo 'Documentation generation not implemented yet'"
  },
  "keywords": [
    "frontend",
    "universidad",
    "inscripciones",
    "pagos"
  ],
  "author": "Arquitecto de Software",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2",
    "eslint": "^8.0.0"
  }
}